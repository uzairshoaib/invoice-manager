<h2>Edit invoice</h2>

<%= render "form.html", changeset: @changeset,
                        action: invoice_path(@conn, :update, @invoice),
                        clients: @clients,
                        types: @types,
                        units: @units %>

<span><%= link "Back", to: invoice_path(@conn, :index) %></span>
